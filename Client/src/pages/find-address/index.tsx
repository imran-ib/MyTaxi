import React from 'react';
import { AuthProvider } from '../../utils/auth';

const FindAddressPage = () => {
  return <AuthProvider>FindAddressPage</AuthProvider>;
};

export default FindAddressPage;
